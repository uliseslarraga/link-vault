apiVersion: v1
kind: Service
metadata:
  name: {{ include "link-vault.fullname" . }}
  labels:
    {{- include "link-vault.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "link-vault.selectorLabels" . | nindent 4 }}
